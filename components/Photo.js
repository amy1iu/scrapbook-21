class Photo {
    constructor(id, prompt, imageBase, user) {
        this.id = id;
        this.prompt = prompt;
        this.imageBase = imageBase;
        this.user = user;
    }
}

export default Photo;